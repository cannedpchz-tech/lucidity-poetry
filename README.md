# Lucidity Poetry v2
Standalone Cloudflare Worker + D1 poetry site.

The first registered account becomes the administrator. Users can create poems and edit/delete only their own poems. Admins can manage all poems.

Deploy with `npx wrangler deploy` from this project directory. Wrangler's configuration is in `wrangler.toml`.
